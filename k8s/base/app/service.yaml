apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    app.kubernetes.io/name: gestion-produits
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gestion-produits
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: 80
